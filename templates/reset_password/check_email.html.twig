{% extends 'base.html.twig' %}

{% block title %}MJ INK | Mail de réinitialisation envoyé{% endblock %}

{% block body %}
<section class="bg-gray-50 h-screen">
  <div class="flex flex-col items-center px-6 py-8 mx-auto md:h-screen">
    <h1 class="font-light text-center font-serif text-3xl mb-10"><a href="{{ path('app_home') }}">MJ INK' TATTOO & BARBER</a></h1>
      <div class="w-full bg-white rounded-lg shadow md:mt-0 sm:max-w-md xl:p-0">
          <div class="p-6 space-y-4 md:space-y-6 sm:p-8">
              <h2 class="text-xl font-bold leading-tight tracking-tight text-gray-900 md:text-2xl text-center">
                  Le mail a bien été envoyé
              </h2>
                  <p class="font-light text-gray-500 text-center">
                    Nous vous avons envoyé un lien de réinitialisation par mail.
                    Ce lien expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                  </p>
                  <p class="font-light text-gray-500 text-center">Si vous n'avez pas reçu de mail, vérifiez dans vos spams ou <a class="font-medium text-primary-600 hover:underline" href="{{ path('app_forgot_password_request') }}">essayez de nouveau</a>.</p>
                    <a class="hover:underline flex justify-center gap-3" href="{{ path('app_login') }}">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M10.5 19.5L3 12m0 0l7.5-7.5M3 12h18" />
                        </svg>
                        <span>Retourner à la page de connexion</span>      
                    </a>
                </div>
          </div>
      </div>
  </div>
</section>
{% endblock %}
